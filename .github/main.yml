yaml

name: 运行脚本

on:

push:

branches:

• 主要的

jobs:

运行任务:

runs-on: ubuntu-latest

steps:

# 1. 拉取代码

• name: 检出代码

uses: actions/checkout@v4

# 2. 设置 Python

• name: 设置 Python 环境

uses: actions/setup-python@v4

with:

python-version: '3.x'

# 3. 运行脚本（这里用的是新名字 main.py）

• name: 执行 Python

run: python main.py
